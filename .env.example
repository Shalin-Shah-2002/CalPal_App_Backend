# ===================================
# Server Configuration
# ===================================
NODE_ENV=development
PORT=3000

# ===================================
# PostgreSQL Database Configuration
# ===================================
# Example: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost:5432/calorie_db

# ===================================
# Appwrite Configuration (Server-Side)
# ===================================
# Appwrite API Endpoint (default: https://cloud.appwrite.io/v1)
APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1

# Your Appwrite Project ID (found in Appwrite Console > Settings)
APPWRITE_PROJECT_ID=your_project_id_here

# Appwrite API Key (Server Key with appropriate permissions)
# SECURITY WARNING: NEVER expose this to frontend!
# Create in: Appwrite Console > Settings > API Keys
# Required Scopes: users.read, sessions.read
APPWRITE_API_KEY=your_api_key_here

# ===================================
# JWT Configuration (Backend Tokens)
# ===================================
# Secret key for signing backend JWTs
# Generate a strong random string (e.g., openssl rand -base64 64)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# JWT expiration time (e.g., 7d, 24h, 60m)
JWT_EXPIRES_IN=7d

# ===================================
# CORS Configuration
# ===================================
# Allowed origins for CORS (comma-separated for multiple origins)
# For Flutter mobile app, use '*' in development
# In production, specify your app's domain
CORS_ORIGIN=*

# ===================================
# AI Configuration (Existing)
# ===================================
# Google Gemini API Key for nutrition data
GEMINI_API_KEY=your_gemini_api_key_here

# ===================================
# How to Set Up Appwrite
# ===================================
# 1. Create account at https://cloud.appwrite.io
# 2. Create new project
# 3. Go to Settings > API Keys > Create API Key
# 4. Enable scopes: users.read, sessions.read
# 5. Copy Project ID and API Key to this file
# 6. Enable authentication methods in Appwrite Console:
#    - Auth > Settings > Enable Email/Password
#    - Auth > Settings > Enable Google OAuth
# 7. Add your Flutter app package name/bundle ID in Appwrite Console

# ===================================
# Security Notes
# ===================================
# - NEVER commit this file with real credentials
# - Keep .env in .gitignore
# - Use different credentials for development and production
# - Rotate API keys regularly
# - Use environment variables in production (Azure, Vercel, etc.)
